# Requirements for RunPod Training Environment
# Optimized for PyTorch containers with CUDA 12.1+

# Core ML Libraries (usually pre-installed on RunPod PyTorch images)
torch>=2.0.0
transformers>=4.51.0
peft>=0.7.0
bitsandbytes>=0.41.0
accelerate>=0.21.0
datasets>=2.14.0
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.4.0

# Experiment Tracking
wandb>=0.15.0

# Data Processing (needed if preparing data on RunPod)
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
jsonlines>=4.0.0

# Evaluation (if running eval on RunPod)
scikit-learn>=1.3.0
scipy>=1.11.0
sentence-transformers>=2.2.0

# LLM APIs (if evaluating on RunPod)
anthropic>=0.18.0

# Utilities
python-dateutil>=2.8.2
pyyaml>=6.0

# Development
ipython>=8.14.0

# Note: Data collection libraries (apify-client, httpx, etc.) not needed on RunPod
# Those are only for your Mac during data collection phase

